const toggle=document.querySelector("#showPassword");const password=document.querySelector("#password");if(toggle&&password)toggle.addEventListener("click",()=>{password.type=password.type==="password"?"text":"password";toggle.textContent=password.type==="password"?"Покажи":"Скрий"});const form=document.querySelector("#loginForm");if(form)form.addEventListener("submit",e=>{e.preventDefault();document.querySelector("#message").textContent="Реалният вход ще бъде активиран при Cloudflare интеграцията."});const menu=document.querySelector(".hamburger");if(menu)menu.addEventListener("click",()=>document.querySelector("aside").classList.toggle("open"));  
