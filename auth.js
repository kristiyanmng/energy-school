const ENERGY_API="https://energy-school-api.energypublishing.workers.dev";const realLoginForm=document.querySelector("#loginForm");if(realLoginForm){realLoginForm.addEventListener("submit",async event=>{event.preventDefault();event.stopImmediatePropagation();const button=document.querySelector("#loginButton"),message=document.querySelector("#message"),identifier=document.querySelector("#email").value.trim(),password=document.querySelector("#password").value;message.className="login-message";message.textContent="Проверка на данните...";button.disabled=true;button.textContent="Влизане...";try{const response=await fetch(ENERGY_API+"/api/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({identifier,password,schoolCode:"ENERGY-DEMO"})});const data=await response.json();if(!response.ok)throw new Error(data.error||"Входът е неуспешен.");const storage=document.querySelector("#rememberLogin").checked?localStorage:sessionStorage;localStorage.removeItem("energySchoolToken");sessionStorage.removeItem("energySchoolToken");storage.setItem("energySchoolToken",data.token);storage.setItem("energySchoolUser",JSON.stringify(data.user));location.href=data.redirect}catch(error){message.className="login-message error";message.textContent=error.message||"Няма връзка със сървъра.";button.disabled=false;button.textContent="Вход в Energy School →"}} ,true)}
