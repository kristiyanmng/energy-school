function renderParentFinals(finals){if(!finals.length)return;const labels={"1":"I срок","2":"II срок",year:"Годишна"},bySubject={};for(const f of finals)(bySubject[f.subject_id]??=[]).push(f);document.querySelectorAll("#parentGradeCards .subject-grade-card").forEach(card=>{const heading=card.querySelector("h2")?.textContent,subject=Object.values(bySubject).find(items=>items[0].subject_name===heading);if(subject)card.insertAdjacentHTML("beforeend",`<div class="final-grade-strip">${subject.map(f=>`<span>${labels[f.term]} <b>${f.value}</b></span>`).join("")}</div>`)})}
